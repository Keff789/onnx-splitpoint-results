# Evaluation evidence

This directory receives:

- the v2.78.4 seven-model reconciliation v2;
- selected paper-comparison reports;
- a compact `biggerset` diagnostic fixture.

The `biggerset` material documents a failed workflow and must stay labelled
`diagnostic`. It contains no valid energy measurement from that run.


# Reproduction scripts

The collection script copies the small A/B and product-smoke scripts that were
actually used. Compiled binaries, build directories and complete tool source
snapshots remain outside this result repository.


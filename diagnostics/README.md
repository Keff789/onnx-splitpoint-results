# Diagnostic fixtures

This area contains compact evidence from failed or deliberately diagnostic
runs. Nothing below this directory is a claim-eligible benchmark unless a
later, explicitly documented rerun changes that status.

